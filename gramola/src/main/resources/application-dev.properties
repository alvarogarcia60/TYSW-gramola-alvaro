# Development profile overrides

# Stripe secret key: prefer environment variable; fallback is a safe placeholder for dev only.
stripe.secret=${STRIPE_SECRET_KEY:sk_test_TU_CLAVE_DEV}

# (Optional) MySQL dev overrides via environment variables
# spring.datasource.url=${MYSQL_URL}
# spring.datasource.username=${MYSQL_USER}
# spring.datasource.password=${MYSQL_PASSWORD}
